'use strict'
ChatRoom.controller 'PrivateRoomCtrl', [
  '$scope', 
  '$modal', 
  '$sails', 
  '$log', 
  'filterFilter', 
  ($scope, $modal, $sails, $log, filterFilter)->

    # $scope.openModal = ()=>
    #   console.log($sails.socket)
    #   console.log $modal
    #   $log.debug('xxxxx')
  ]